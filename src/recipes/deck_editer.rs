// cards_statusの中からfalseのカードを検索
pub(crate) fn pull_card(
    cards_status: [bool; 52],
    hands_player: Vec<u32>,
    hands_dealer: Vec<u32>,
    add_pleyer: i32,
    add_dealer: i32,
) -> () {
    todo!()
}
